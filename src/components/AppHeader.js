/**
 * Clinician pathway header — shared program bar (brand, part/step, care team, reset).
 * Implementation lives in ProgramHeaderBar.js for reuse (role picker, shell, etc.).
 */
export { default } from './ProgramHeaderBar.js'
